{
  "name": "travel-log",
  "version": "0.1.0",
  "private": true,
  "author": "Waheed Afolabi <waheed.remote247@gmail.com>",
  "license": "MIT",
  "scripts": {
    "client": "cd client && yarn build --prefix client",
    "server": "cd server && yarn start --prefix server",
    "start:dev": "concurrently 'yarn server' 'yarn client'",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false yarn --prefix client && yarn build --prefix"
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
